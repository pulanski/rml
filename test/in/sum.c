#include "sum.h"

// Generated C Program

// Struct definition omitted in source file

enum Bar { A, B };
// Ignore module definition as C does not have a module system.

int sum(int a, int b) {
  for (int i = 0; 3; i++) {
    if ((i == 5)) {
      break;
    }
  }
}
int main() {
  int b = 20;
  int a = b;
  while (1) {
  }
  int e = 42;
}
struct Trait {
  int (*foo)();
};